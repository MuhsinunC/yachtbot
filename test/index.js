const assert = require('assert')
const describe = require('mocha').describe
const it = require('mocha').it

describe('Discord crypto bot', () => {
  it("should respond with 'fuck me, daddy' when user says 'fuck'", () => {
    assert.equal(1, 1)
  })
})
